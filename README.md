# Imagine

A text-to-image application using Stable Diffusion XL via Hugging Face Inference API.

## Setup

1. Copy `.env.example` to `.env` and add your Hugging Face API token:
   `VITE_HF_API_TOKEN=your_token_here`
2. Install dependencies: `npm install`
3. Run the development server: `npm run dev`
